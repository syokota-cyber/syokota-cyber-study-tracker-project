# Lambda用依存関係（認証統合版）
# 基本機能
boto3>=1.34.0

# JWT認証（標準ライブラリベース - 外部依存関係なし）
# PyJWT>=2.8.0  # 削除 - 標準ライブラリで実装
# cryptography>=41.0.0  # 削除 - 標準ライブラリで実装

# DynamoDB対応
dynamodb-encryption-sdk>=3.3.0

# 将来的なFastAPI統合の準備
# fastapi>=0.104.0
# mangum>=0.17.0
# pydantic>=2.5.0 